name: Hello World
on:
  push:
    branches: [main]
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello from GitHub Actions!"
      - name: Show the date
        run: date
      - name: List files
        run: ls -la# This is a basic workflow to help you get started with Actions

